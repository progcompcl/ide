{
  "name": "compiler-wasm",
  "type": "module",
  "version": "1.0.0",
  "description": "C++ WebAssembly compiler running entirely in the browser",
  "scripts": {
    "dev": "bunx --bun astro dev",
    "build": "bunx --bun astro build",
    "preview": "bunx --bun astro preview",
    "astro": "bunx --bun astro"
  },
  "dependencies": {
    "ace-builds": "^1.43.4",
    "astro": "5.15.5"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  }
}